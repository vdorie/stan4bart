An unordered backlog; history lives in docs/plans/, docs/design/, and git.

items
  callback-estimators: TABLED with its consumer (bartCause) 2026-07-16.
    The design and measurements are complete in
    docs/plans/callback-estimators.md (per-draw reducing callbacks
    store draws x estimators instead of n x draws surfaces; prototype
    +1.1 percent wall, bitwise ate; the stan4bart-side contract
    refinements - assembled response-scale ev to the callback, warmup
    gating, documenting the seam - are consumer-gated and land with
    adoption).
  cleanup-file-names: source-file naming pass (pre-WALNUTS entry,
    still applicable to the surviving files).
  perf-followups: the ranked candidates from the landed runtime review
    (docs/plans/perf-review.md, measured 2026-07-16): the WALNUTS
    warmup/leapfrog-count lever (adequate warmup already halves
    large-n mixed per-iteration wall, 2.2x; any tuning change is
    distributional-gate-bound), ParametricModel::eval scratch reuse
    (the buffer-reuse half is numerically inert; eval is 24-40% of
    per-iteration compute), fitted() apply -> rowMeans, the optional
    ppd-mean short-circuit, and extract() transient-array trims.
    Setup/marshalling/assembly measured as fixed-cost and NOT worth
    attacking; the BART 54-72% share is routed to dbarts.
  store-trees-adaptive-default: revisit making store = "trees" the
    default above a size threshold once the opt-in's contracts are
    lived-in (docs/plans/bart-train-recompute.md C4 resolution); also
    the candidate reference pattern for other dbarts consumers.
